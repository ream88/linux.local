{
  "scripts": {
    "build": "bunx @tailwindcss/cli -i ./src/styles.css -o ./dist/styles.css --minify && bun run copy:assets",
    "copy:assets": "cp src/*.html src/*.png src/*.jpg src/*.jpeg src/*.gif src/*.svg src/*.ico dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "tailwindcss": "^4.1.13"
  }
}
